[project]
name = "repo-benchmanrk"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "github-to-sqlite>=2.9",
    "llm>=0.21",
]
